<%= form_for(@vizitum) do |f| %>
  <% if @vizitum.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vizitum.errors.count, "error") %> prohibited this vizitum from being saved:</h2>

      <ul>
      <% @vizitum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>
  <div class="field">
    <%= f.label :tip %><br />
    <%= f.text_field :tip %>
  </div>
  <div class="field">
    <%= f.label :pacient %><br />
    <%= f.text_field :pacient %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
